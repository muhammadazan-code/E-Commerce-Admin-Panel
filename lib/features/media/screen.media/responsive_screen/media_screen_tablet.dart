import 'package:flutter/material.dart';

class TabletMediaScreen extends StatelessWidget {
  const TabletMediaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
